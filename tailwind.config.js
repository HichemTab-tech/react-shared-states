/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.{html,js,jsx,ts,tsx}'], // Specify files to scan for Tailwind classes
    theme: {
        extend: {}, // Add custom Tailwind configurations here if needed
    },
    plugins: [], // Add Tailwind plugins here if required
};